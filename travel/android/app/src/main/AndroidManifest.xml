  <manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="test_1_2"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <meta-data android:name="flutter_deeplinking_enabled" android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
            <data android:host="www.evaneos.ca"  android:path="/path1"/>
            <data android:host="www.evaneos.ch" android:path="/path2" />
            <data android:host="www.evaneos.co.uk" android:path="/path3" />
            <data android:host="www.evaneos.com" />
            <data android:host="www.evaneos.de" />
            <data android:host="www.evaneos.es" />
            <data android:host="www.evaneos.fr" />
            <data android:host="www.evaneos.it" />
            <data android:host="www.evaneos.nl" />
            <data android:host="branchio.services.evaneos.com" />
            <data android:host="evaneos-alternate.app.link" />
            <data android:host="mobile.links.evaneos.com" />
            <data android:host="evaneos.dk" />
            <data android:host="www.evaneos.dk" />
            <data android:host="evaneos.se" />
            <data android:host="www.evaneos.se" />
            <data android:host="evaneos-mariage.com" />
            <data android:host="fr.evaneos.be" />
            <data android:host="fr.evaneos.ca" />
            <data android:host="fr.evaneos.ch" />
            <data android:host="it.evaneos.ch" />
            <data android:host="www.evaneos-mariage.com" />
            <data android:host="evaneos.ca" />
            <data android:host="evaneos.ch" />
            <data android:host="evaneos.co.uk" />
            <data android:host="evaneos.com" />
            <data android:host="evaneos.de" />
            <data android:host="evaneos.es" />
            <data android:host="evaneos.fr" />
            <data android:host="evaneos.it" />
            <data android:host="evaneos.at" />
            <data android:host="evaneos.nl" />
            <data android:host="evaneos.be" />
            <data android:host="www.evaneos-travel.com" />
            <data android:host="www.evaneos.at" />
            <data android:host="www.evaneos.be" />
            <data android:host="evaneos-travel.com" />
            <data android:host="www.evaneos.net" />
            <data android:host="www.marche.gratis" />

            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility?hl=en and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
  
  
  